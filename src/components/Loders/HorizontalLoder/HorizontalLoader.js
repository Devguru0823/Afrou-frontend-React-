import React from 'react';
import './HorizontalLoader.css';

const horizontalLoader = props => {
  return (
    <div className="horizontal-loader" {...props}>Loading...</div>
  );
};

export default horizontalLoader;